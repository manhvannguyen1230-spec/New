Quick setup notes:

- package.json includes scripts: `dev`, `build`, `preview`.
- TypeScript configured in `tsconfig.json` with `jsx: react-jsx`.
- Vite configuration in `vite.config.ts` uses `@vitejs/plugin-react` for fast refresh and JSX support.

If you'd like I can run `npm install` and start the dev server here (I will first check whether Node/npm is installed).